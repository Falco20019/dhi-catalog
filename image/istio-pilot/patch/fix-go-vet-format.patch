Fix an incorrect formatting verb in an upstream test that is rejected by Go vet.
The value is an integer count, so use %d rather than the string-oriented %q verb.
Upstream source: https://github.com/istio/istio/blob/1.29.6/pilot/pkg/serviceregistry/kube/conversion_test.go#L196

diff --git a/pilot/pkg/serviceregistry/kube/conversion_test.go b/pilot/pkg/serviceregistry/kube/conversion_test.go
--- a/pilot/pkg/serviceregistry/kube/conversion_test.go
+++ b/pilot/pkg/serviceregistry/kube/conversion_test.go
@@ -193,7 +193,7 @@ func TestServiceConversion(t *testing.T) {
 
 	ips := service.ClusterVIPs.GetAddressesFor(clusterID)
 	if len(ips) != 1 {
-		t.Fatalf("number of ips incorrect => %q, want 1", len(ips))
+		t.Fatalf("number of ips incorrect => %d, want 1", len(ips))
 	}
 
 	if ips[0] != ip {
