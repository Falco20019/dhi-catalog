# Pinned runtime dependencies for the sendgrid venv (installed with --no-deps).
# sendgrid itself is installed separately from the pinned upstream git tag.
# Bump these deliberately to remediate CVEs; keep versions in sync across the
# debian (glibc) and alpine (musl) leaves.
python-http-client==3.3.7
cryptography==50.0.1
cffi==2.1.1
pycparser==3.0
werkzeug==3.1.8
markupsafe==3.0.3
