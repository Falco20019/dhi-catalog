# syntax=dhi.io/build:2-alpine3.24

image: dhi.io/pkg-k8s-driver-manager
variant: runtime
tags:
  - 0.12.0-alpine3.24
  - 0.12.0-r1-alpine3.24
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-08-19"
vars:
  COMMIT_SHA: bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
  GOLANG_VERSION: 1.26.8-r0
  PACKAGE_NAME: k8s-driver-manager
  REL: "1"
  SEMVER_MAJOR_MINOR_VERSION: "0.12"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.12.0
  VERSION: 0.12.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.24/main
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.24/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: k8s-driver-manager
      work-dir: /go/src/github.com/NVIDIA/k8s-driver-manager
      contents:
        packages:
          - busybox
          - bash
          - ca-certificates-bundle
          - golang-1.26=1.26.8-r0
          - golang-1.26-oci-config=1.26.8-r0
        files:
          - url: git+https://github.com/NVIDIA/k8s-driver-manager.git#v0.12.0
            checksum: bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
            path: /go/src/github.com/NVIDIA/k8s-driver-manager
            spdx:
              name: k8s-driver-manager
              version: 0.12.0
              packages:
                - name: k8s-driver-manager
                  purl: pkg:golang/github.com/NVIDIA/k8s-driver-manager@0.12.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: remove vendor directory
          runs: |
            set -eux -o pipefail

            rm -rf vendor
        - name: update go dependencies
          uses: go/bump@v2
          with:
            deps: []
        - uses: go/hermetic@v1
        - name: test
          runs: |
            set -eux -o pipefail

            CGO_ENABLED=0 go test -count=1 ./...
        - name: build driver-manager
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.gitCommit=bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.version=0.12.0
            output: ${target.dir}/usr/bin/driver-manager
            packages: ./cmd/driver-manager
        - name: build driver-manager dev
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.gitCommit=bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.version=0.12.0
            output: ${target.dir}/usr/bin/driver-manager-dev
            packages: ./cmd/driver-manager
            strip: false
        - name: build vfio-manage
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.gitCommit=bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.version=0.12.0
            output: ${target.dir}/usr/bin/vfio-manage
            packages: ./cmd/vfio-manage
        - name: build vfio-manage dev
          uses: go/build@v1
          with:
            fips: false
            ldflags:
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.gitCommit=bd62ca7d4a25376a7edd67e2f247e124cd77cd9c
              - -X github.com/NVIDIA/k8s-driver-manager/internal/info.version=0.12.0
            output: ${target.dir}/usr/bin/vfio-manage-dev
            packages: ./cmd/vfio-manage
            strip: false
        - uses: dhi/build-pkg:0
          with:
            apk:
              depends:
                - busybox
            deb:
              depends:
                - coreutils
            depends:
              - vfio-manage
            description: NVIDIA Driver Manager for Kubernetes, manages NVIDIA driver upgrades on GPU Operator nodes
            files:
              - usr/bin/driver-manager
            license: Apache-2.0
            name: k8s-driver-manager
            rel: 1
            subpackages:
              - description: Manage VFIO driver binding for NVIDIA GPU devices
                files:
                  - path: usr/bin/vfio-manage
                name: vfio-manage
              - depends:
                  - vfio-manage-dev
                description: driver-manager binary without strip (dev)
                files:
                  - path: usr/bin/driver-manager
                    source: usr/bin/driver-manager-dev
                name: k8s-driver-manager-dev
              - description: vfio-manage binary without strip (dev)
                files:
                  - path: usr/bin/vfio-manage
                    source: usr/bin/vfio-manage-dev
                name: vfio-manage-dev
            url: https://github.com/NVIDIA/k8s-driver-manager
            version: 0.12.0
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.24"
  pretty-name: Docker Hardened Images/Alpine Linux v3.24
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
